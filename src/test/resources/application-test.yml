spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;NON_KEYWORDS=VALUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false
  mail:
    host: localhost
    port: 25

jwt:
  secret: test-jwt-secret-key-for-unit-test-only-minimum-32-characters

file:
  base-url: http://localhost:8080
  upload-dir: uploads/attendance

app:
  cors:
    allowed-origins: http://localhost:3000

ncp:
  sms:
    access-key: test
    secret-key: test
    service-id: test
    sender: test